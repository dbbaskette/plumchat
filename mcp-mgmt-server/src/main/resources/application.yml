server:
  port: 8083
management:
  endpoints:
    web:
      exposure:
        include: health,info
plumchat:
  mgmt:
    enabled: false
    allowed-hosts: []
    allowed-users: []
    allowed-key-dirs: ["/etc/plumchat/keys", "/opt/plumchat/keys"]


